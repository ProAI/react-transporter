import isEqual from './isEqual';
import isInArray from './isInArray';
import push from './push';
import slice from './slice';
import enforceArray from './enforceArray';

export { isEqual, isInArray, push, slice, enforceArray };
