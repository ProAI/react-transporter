export default function createMetaHandler(data) {
  // TODO
  console.log(data);

  // 1) Cache current meta data
  // 2) Apply new meta data

  // TODO
  return {
    destroy() {
      // Apply meta data from cache
    },
  };
}
